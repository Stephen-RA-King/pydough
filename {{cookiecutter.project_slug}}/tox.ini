[tox]
envlist = linter,py36,py37,py38

[testenv]
deps =
    -r requirements.txt
commands =
    pip install -e .
    pytest .

[testenv:linter]
deps =
    -r requirements.txt
commands =
    flake8
    black --check .
    pydocstyle
    mypy .
